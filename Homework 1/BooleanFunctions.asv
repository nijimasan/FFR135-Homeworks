% Maximilian Salén 970105-1576
clear all;close all;clc


% Initialization
n = [2 3 4 5];
nTrials = 1e4;
nEpochs = 20;
eta = 0.05;
counter = 0;

booleanInputs = [0 1 0 1;0 0 1 1]';

usedBool = [];
for dimension
for trial = 1:nTrials
    randomBoolean = randi([0, 1], 2^n, 1);
    randomBoolean(randomBoolean==0) = -1;

    if randomBool 
end